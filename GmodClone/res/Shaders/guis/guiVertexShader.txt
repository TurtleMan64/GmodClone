#version 140

in vec2 position;

out vec2 textureCoords;

uniform mat4 transformationMatrix;

void main(void)
{
    vec4 aef = transformationMatrix * vec4(position, 0.0, 1.0);
	gl_Position = vec4(2*aef.x - 1, -2*aef.y + 1, aef.z, aef.w);
	textureCoords = position;
}
